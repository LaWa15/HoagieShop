<%= form_with(model: ingredient, local: true) do |form| %>
  <% if ingredient.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ingredient.errors.count, "error") %> prohibited this ingredient from being saved:</h2>

      <ul>
      <% ingredient.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :ingredient_name %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.number_field :price, id: :ingredient_price %>
  </div>

  <div class="field">
    <%= form.label :maxUsage %>
    <%= form.number_field :maxUsage, id: :ingredient_maxUsage %>
  </div>

  <div class="field">
    <%= form.label :available %>
    <%= form.check_box :available, id: :ingredient_available %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
